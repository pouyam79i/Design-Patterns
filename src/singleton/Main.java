package singleton;

// test
public class Main {
    public static void main(String[] args) {
        Singleton singleton = Singleton.getOnlyInstance();
//        Singleton singleton1 = new Singleton();     // This line of code will not be compiled
    }
}
